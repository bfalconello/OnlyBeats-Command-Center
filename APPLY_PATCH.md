# Apply OnlyBeats v6.0.3

1. Copy the complete v6.0.3 package over the repository.
2. Run RUN_DESKTOP.bat and confirm QA remains green.
3. Build the installer and update metadata.
4. Commit the release.
5. Push tag v6.0.3.
6. Publish the GitHub Release with the installer, blockmap, and latest.yml.
7. Test the update from the installed v6.0.2 application.

Suggested commit:

`v6.0.3: add automatic update verification marker`
