# Apply M3.1 — Live Game Focus

Copy into the repository:

- `app/app.js`
- `app/index.html`
- the complete `app/modules/` folder
- `docs/M3_1_LIVE_GAME_FOCUS.md`

Replace files when prompted. Preserve `.git`, CSS, assets, Rust files, and local application data.

Suggested commit:

`M3.1: build connected Live Game Focus workspace`
