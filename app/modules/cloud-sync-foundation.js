'use strict';
const CLOUD_SYNCABLE_PREFIXES=['onlybeats.predictions','onlybeats.futures','onlybeats.favorites','onlybeats.settings','onlybeats.availability','onlybeats.season-archive','onlybeats.timeline','onlybeats.live-alerts','onlybeats.command-center','onlybeats.gameday-command','onlybeats.ui-quality','onlybeats.desktop-state'];
let cloudSyncState={provider:'none',connected:false,accountEmail:'',accountId:'',lastSyncAt:null,lastPullAt:null,lastPushAt:null,conflictPolicy:'newest-wins',autoSync:false,status:'Not connected',error:''};
let cloudQueue=[],cloudActivity=[],cloudSyncInFlight=false;
function loadCloudSyncState(){try{cloudSyncState={...cloudSyncState,...JSON.parse(localStorage.getItem(CLOUD_SYNC_KEY)||'{}')}}catch{};try{const x=JSON.parse(localStorage.getItem(CLOUD_QUEUE_KEY)||'[]');cloudQueue=Array.isArray(x)?x:[]}catch{cloudQueue=[]};try{const x=JSON.parse(localStorage.getItem(CLOUD_ACTIVITY_KEY)||'[]');cloudActivity=Array.isArray(x)?x:[]}catch{cloudActivity=[]}}
function saveCloudSyncState(){localStorage.setItem(CLOUD_SYNC_KEY,JSON.stringify(cloudSyncState));localStorage.setItem(CLOUD_QUEUE_KEY,JSON.stringify(cloudQueue.slice(-500)));localStorage.setItem(CLOUD_ACTIVITY_KEY,JSON.stringify(cloudActivity.slice(-300)))}
function cloudRecordActivity(type,message,detail=''){cloudActivity.push({id:`${Date.now()}-${Math.random().toString(36).slice(2,8)}`,time:new Date().toISOString(),type,message,detail});if(cloudActivity.length>300)cloudActivity=cloudActivity.slice(-300);saveCloudSyncState()}
function cloudConfigPresent(){return Boolean(window.ONLYBEATS_FIREBASE_CONFIG?.apiKey&&window.ONLYBEATS_FIREBASE_CONFIG?.projectId)}
function cloudProviderStatus(){if(cloudSyncInFlight)return'Syncing';if(!navigator.onLine)return'Offline';if(cloudSyncState.error)return'Error';if(cloudSyncState.connected)return'Connected';if(cloudConfigPresent())return'Configured';return'Not configured'}
function cloudSyncableKeys(){const keys=[];for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i);if(key&&CLOUD_SYNCABLE_PREFIXES.some(prefix=>key.startsWith(prefix)))keys.push(key)}return keys.sort()}
function cloudLocalSnapshot(){const records={};for(const key of cloudSyncableKeys())records[key]={value:localStorage.getItem(key),updatedAt:new Date().toISOString(),deviceId:crossDeviceState?.deviceId||'unknown-device'};return{schemaVersion:1,appVersion:VERSION,generatedAt:new Date().toISOString(),deviceId:crossDeviceState?.deviceId||'unknown-device',records}}
function cloudQueueChange(key,value,operation='set'){const item={id:`${Date.now()}-${Math.random().toString(36).slice(2,8)}`,key,value,operation,queuedAt:new Date().toISOString(),deviceId:crossDeviceState?.deviceId||'unknown-device',attempts:0};const i=cloudQueue.findIndex(x=>x.key===key);if(i>=0)cloudQueue.splice(i,1,item);else cloudQueue.push(item);saveCloudSyncState()}
function installCloudStorageObserver(){if(Storage.prototype.__onlybeatsCloudWrapped)return;const set=Storage.prototype.setItem,remove=Storage.prototype.removeItem;Storage.prototype.setItem=function(key,value){const result=set.call(this,key,value);if(this===localStorage&&typeof key==='string'&&CLOUD_SYNCABLE_PREFIXES.some(p=>key.startsWith(p)))cloudQueueChange(key,String(value),'set');return result};Storage.prototype.removeItem=function(key){const result=remove.call(this,key);if(this===localStorage&&typeof key==='string'&&CLOUD_SYNCABLE_PREFIXES.some(p=>key.startsWith(p)))cloudQueueChange(key,null,'remove');return result};Storage.prototype.__onlybeatsCloudWrapped=true}
function createCloudAdapter(){return window.ONLYBEATS_CLOUD_ADAPTER||{name:'No backend',configured:false,async connect(){throw new Error('No cloud adapter is connected. Add firebase-config.js and a compatible adapter.')},async push(){throw new Error('Cloud push unavailable.')},async pull(){throw new Error('Cloud pull unavailable.')},async disconnect(){return true}}}
function cloudConflictDecision(localRecord,remoteRecord){if(cloudSyncState.conflictPolicy==='local-wins')return'local';if(cloudSyncState.conflictPolicy==='remote-wins')return'remote';return new Date(remoteRecord?.updatedAt||0)>new Date(localRecord?.updatedAt||0)?'remote':'local'}
function cloudApplyRemoteSnapshot(snapshot){if(!snapshot||typeof snapshot.records!=='object')throw new Error('Remote snapshot is invalid.');let applied=0,skipped=0;for(const [key,remote] of Object.entries(snapshot.records)){const local=localStorage.getItem(key);const decision=local===null?'remote':cloudConflictDecision({value:local,updatedAt:cloudSyncState.lastPushAt||0},remote);if(decision==='remote'){remote.value==null?localStorage.removeItem(key):localStorage.setItem(key,String(remote.value));applied++}else skipped++}return{applied,skipped}}
async function cloudConnect(){const adapter=createCloudAdapter();cloudSyncInFlight=true;cloudSyncState.error='';try{const account=await adapter.connect(window.ONLYBEATS_FIREBASE_CONFIG||{});cloudSyncState.provider=adapter.name||'Configured adapter';cloudSyncState.connected=true;cloudSyncState.accountEmail=account?.email||'';cloudSyncState.accountId=account?.id||account?.uid||'';cloudSyncState.status='Connected';cloudRecordActivity('connect','Cloud provider connected',cloudSyncState.accountEmail||cloudSyncState.accountId);return true}catch(e){cloudSyncState.connected=false;cloudSyncState.status='Connection failed';cloudSyncState.error=e?.message||String(e);cloudRecordActivity('error','Cloud connection failed',cloudSyncState.error);return false}finally{cloudSyncInFlight=false;saveCloudSyncState()}}
async function cloudPushNow(){if(!cloudSyncState.connected)throw new Error('Connect a cloud provider before pushing data.');const adapter=createCloudAdapter();cloudSyncInFlight=true;try{const snapshot=cloudLocalSnapshot();await adapter.push({accountId:cloudSyncState.accountId,snapshot,queue:[...cloudQueue],device:crossDeviceState});cloudQueue=[];cloudSyncState.lastPushAt=cloudSyncState.lastSyncAt=new Date().toISOString();cloudSyncState.status='Synced';cloudRecordActivity('push','Cloud push completed',`${Object.keys(snapshot.records).length} records`);return true}catch(e){cloudSyncState.error=e?.message||String(e);cloudSyncState.status='Push failed';cloudQueue=cloudQueue.map(x=>({...x,attempts:(x.attempts||0)+1}));cloudRecordActivity('error','Cloud push failed',cloudSyncState.error);return false}finally{cloudSyncInFlight=false;saveCloudSyncState()}}
async function cloudPullNow(){if(!cloudSyncState.connected)throw new Error('Connect a cloud provider before pulling data.');const adapter=createCloudAdapter();cloudSyncInFlight=true;try{const result=cloudApplyRemoteSnapshot(await adapter.pull({accountId:cloudSyncState.accountId,device:crossDeviceState}));cloudSyncState.lastPullAt=cloudSyncState.lastSyncAt=new Date().toISOString();cloudSyncState.status='Synced';cloudRecordActivity('pull','Cloud pull completed',`${result.applied} applied · ${result.skipped} kept local`);return result}catch(e){cloudSyncState.error=e?.message||String(e);cloudSyncState.status='Pull failed';cloudRecordActivity('error','Cloud pull failed',cloudSyncState.error);return false}finally{cloudSyncInFlight=false;saveCloudSyncState()}}
async function cloudDisconnect(){try{await createCloudAdapter().disconnect()}catch{}cloudSyncState.connected=false;cloudSyncState.accountEmail='';cloudSyncState.accountId='';cloudSyncState.status='Disconnected';cloudRecordActivity('disconnect','Cloud provider disconnected');saveCloudSyncState()}
function cloudQueueSummary(){return{total:cloudQueue.length,sets:cloudQueue.filter(x=>x.operation==='set').length,removals:cloudQueue.filter(x=>x.operation==='remove').length,retries:cloudQueue.reduce((s,x)=>s+(x.attempts||0),0)}}
function cloudActivityRow(item){const icon=item.type==='error'?'×':item.type==='push'?'↑':item.type==='pull'?'↓':'•';return `<div class="intel-row"><span class="intel-icon">${icon}</span><div><strong>${esc(item.message)}</strong><small>${new Date(item.time).toLocaleString()}${item.detail?` · ${esc(item.detail)}`:''}</small></div><span class="provider-badge">${esc(item.type.toUpperCase())}</span></div>`}
function cloudSyncPage(){setHeading('Cloud Sync','ACCOUNT READY · OFFLINE QUEUE · CONFLICT POLICY');const provider=createCloudAdapter(),queue=cloudQueueSummary(),status=cloudProviderStatus(),configured=cloudConfigPresent()||provider.configured;return `<section class="intel-hero"><div><p class="eyebrow">ONLYBEATS CLOUD PLATFORM</p><h2>${cloudSyncState.connected?'Cloud account connected.':configured?'Cloud configuration detected.':'Cloud backend not configured yet.'}</h2><p>The sync engine, offline queue, conflict policy, and adapter boundary are ready. Remote sync activates only after a supported backend configuration is added.</p></div><div class="button-row"><button class="button primary" id="cloudConnect" ${configured&&!cloudSyncState.connected?'':'disabled'}>Connect provider</button><button class="button" id="cloudPush" ${cloudSyncState.connected?'':'disabled'}>Push now</button><button class="button" id="cloudPull" ${cloudSyncState.connected?'':'disabled'}>Pull now</button><button class="button" id="cloudDisconnect" ${cloudSyncState.connected?'':'disabled'}>Disconnect</button></div></section><div class="metric-grid">${metric('Status',status,cloudSyncState.status)}${metric('Provider',cloudSyncState.provider||provider.name,configured?'Configuration available':'No backend')}${metric('Account',cloudSyncState.accountEmail||'Not signed in',cloudSyncState.accountId||'No account ID')}${metric('Pending Queue',queue.total,`${queue.sets} updates · ${queue.removals} removals`)}${metric('Last Sync',cloudSyncState.lastSyncAt?new Date(cloudSyncState.lastSyncAt).toLocaleTimeString():'Never','Current device')}${metric('Conflict Policy',cloudSyncState.conflictPolicy,'Applied during pull')}</div>${cloudSyncState.error?`<div class="provider-notice"><div><strong>Cloud sync needs attention</strong><p class="muted">${esc(cloudSyncState.error)}</p></div></div>`:''}<div class="reports-grid">${card('Backend Readiness',`<div class="detail-list"><div><span>Firebase config</span><strong>${cloudConfigPresent()?'Detected':'Missing'}</strong></div><div><span>Cloud adapter</span><strong>${provider.configured?'Configured':'Not connected'}</strong></div><div><span>Online status</span><strong>${navigator.onLine?'Online':'Offline'}</strong></div><div><span>Automatic sync</span><strong>${cloudSyncState.autoSync?'Enabled':'Disabled'}</strong></div><div><span>Offline queue</span><strong>Ready</strong></div><div><span>Remote database</span><strong>${cloudSyncState.connected?'Connected':'Not connected'}</strong></div></div><label class="toggle-row"><span>Automatic sync when connected</span><input type="checkbox" id="cloudAutoSync" ${cloudSyncState.autoSync?'checked':''}></label>`)}${card('Conflict Policy',`<div class="detail-list"><label><span>Resolution method</span><select id="cloudConflictPolicy"><option value="newest-wins" ${cloudSyncState.conflictPolicy==='newest-wins'?'selected':''}>Newest change wins</option><option value="local-wins" ${cloudSyncState.conflictPolicy==='local-wins'?'selected':''}>Keep this device</option><option value="remote-wins" ${cloudSyncState.conflictPolicy==='remote-wins'?'selected':''}>Keep cloud version</option></select></label><div><span>Queued retries</span><strong>${queue.retries}</strong></div><div><span>Last push</span><strong>${cloudSyncState.lastPushAt?new Date(cloudSyncState.lastPushAt).toLocaleString():'Never'}</strong></div><div><span>Last pull</span><strong>${cloudSyncState.lastPullAt?new Date(cloudSyncState.lastPullAt).toLocaleString():'Never'}</strong></div></div>`)}${card('Offline Change Queue',cloudQueue.length?`<div class="intel-list">${cloudQueue.slice().reverse().slice(0,100).map(item=>`<div class="intel-row"><span class="intel-icon">${item.operation==='remove'?'−':'+'}</span><div><strong>${esc(item.key)}</strong><small>${new Date(item.queuedAt).toLocaleString()} · ${esc(item.operation)} · Attempts ${item.attempts||0}</small></div></div>`).join('')}</div>`:empty('No queued changes','Local changes will be queued here for the cloud provider.'),'wide')}${card('Sync Activity',cloudActivity.length?`<div class="intel-list">${cloudActivity.slice().reverse().slice(0,100).map(cloudActivityRow).join('')}</div>`:empty('No cloud activity yet','Connection, push, pull, and error events will appear here.'),'wide')}${card('Configuration Steps',`<div class="intel-list"><div class="intel-row"><span class="intel-icon">1</span><div><strong>Create a Firebase project</strong><small>Enable Authentication and Firestore in your own Firebase account.</small></div></div><div class="intel-row"><span class="intel-icon">2</span><div><strong>Copy the example config</strong><small>Rename firebase-config.example.js to firebase-config.js and add your project values.</small></div></div><div class="intel-row"><span class="intel-icon">3</span><div><strong>Add the adapter implementation</strong><small>Expose window.ONLYBEATS_CLOUD_ADAPTER with connect, push, pull, and disconnect methods.</small></div></div><div class="intel-row"><span class="intel-icon">4</span><div><strong>Test security rules</strong><small>Verify account isolation before storing real user data.</small></div></div></div>`,'wide')}</div>`}
function bindCloudSync(){if($('cloudConnect'))$('cloudConnect').onclick=async()=>{const ok=await cloudConnect();renderPage();toast(ok?'Cloud provider connected':'Cloud connection failed',ok?'success':'error')};if($('cloudPush'))$('cloudPush').onclick=async()=>{try{const ok=await cloudPushNow();renderPage();toast(ok?'Cloud push completed':'Cloud push failed',ok?'success':'error')}catch(e){toast(e?.message||'Cloud push failed','error')}};if($('cloudPull'))$('cloudPull').onclick=async()=>{try{const result=await cloudPullNow();renderPage();if(result)toast(`Cloud pull completed: ${result.applied} applied`,'success')}catch(e){toast(e?.message||'Cloud pull failed','error')}};if($('cloudDisconnect'))$('cloudDisconnect').onclick=async()=>{await cloudDisconnect();renderPage();toast('Cloud provider disconnected')};if($('cloudAutoSync'))$('cloudAutoSync').onchange=e=>{cloudSyncState.autoSync=e.target.checked;saveCloudSyncState();toast(`Automatic sync ${cloudSyncState.autoSync?'enabled':'disabled'}`)};if($('cloudConflictPolicy'))$('cloudConflictPolicy').onchange=e=>{cloudSyncState.conflictPolicy=e.target.value;saveCloudSyncState();toast('Conflict policy updated')}}
function cloudAutoSyncTick(){if(!cloudSyncState.autoSync||!cloudSyncState.connected||cloudSyncInFlight||!navigator.onLine||cloudQueue.length===0)return;cloudPushNow().then(ok=>{if(ok&&currentPage==='cloud')renderPage()})}
function initializeCloudSyncFoundation(){loadCloudSyncState();installCloudStorageObserver();window.addEventListener('online',()=>{cloudRecordActivity('network','Network restored');cloudAutoSyncTick()});window.addEventListener('offline',()=>cloudRecordActivity('network','Network offline'));setInterval(cloudAutoSyncTick,60000)}
